name: card_input_field
description: A new Flutter package.
version: 0.0.1
homepage: https://github.com/HosseinAsadi/card_input_field

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
